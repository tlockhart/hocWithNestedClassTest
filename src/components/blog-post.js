import React, {Component, useEffect, useState} from 'react';
import Model from "../utils/Model";
import data from "../utils/data";

interface BlogPostArgs {
    title,
    body,
}

class BlogPost extends Component<BlogPostArgs>{
    return();
}
export default BlogPost;